class ProductArguments {
  final String productId;
  final String name;
  final double price;
  final String supplierId;

  ProductArguments(this.productId, this.name, this.price, this.supplierId);
}
